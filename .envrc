# Use hermes-agent's nix devshell — provides Python, aiohttp, and all gateway.* modules.
use flake /home/lightx/source/hermes-agent

# Ensure hermes-agent source is importable (gateway.config, gateway.platforms.base, etc.)
export PYTHONPATH=/home/lightx/source/hermes-agent:${PYTHONPATH:-}
